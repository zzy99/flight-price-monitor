name: schedule-test

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Print time and event
        run: |
          echo "RUN AT: $(date -u)"
          echo "GITHUB_EVENT_NAME = $GITHUB_EVENT_NAME"
